fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
python-multipart>=0.0.12
Pillow>=10.0.0
ultralytics>=8.0.0
# Needed for `from google import genai`
google-genai>=0.3.0
torchvision>=0.15.0
# Keep transformers below versions that enforce torch>=2.6 on load.
transformers>=4.41.0,<4.52
numpy<2
opencv-python<4.11
# LLaVA navigation (ENABLE_LLAVA_NAV=1): needs accelerate + transformers above.
accelerate>=0.26.0
# bitsandbytes: ONLY for 4-bit LLaVA on Linux+NVIDIA (LLAVA_USE_4BIT=1). macOS PyPI wheels stop at ~0.42;
# transformers may require newer for 4-bit — on Mac default is FP16/MPS via LLAVA_USE_4BIT=0 (see llava_navigation.py).
